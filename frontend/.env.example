# ──────────────────────────────────────────────────────────────
# 勞基法小幫手 — 前端環境變數範本
# 複製此檔為 .env.local 並填入真實數值後即可啟動前端。
# .env.local 已列入 .gitignore，不會上傳到 GitHub。
# ──────────────────────────────────────────────────────────────

# 後端 API 位址
# 本機開發：http://localhost:8000（或依實際 port 調整）
# Render 部署後：https://your-app.onrender.com
NEXT_PUBLIC_API_URL=https://your-app.onrender.com

# 應用模式
# opensource = 開源/公開版（AI 功能為複製提示詞→ ChatGPT/Gemini，不吃 LLM quota）
# private    = 親友完整版（直接呼叫後端 Gemini API，需有效 GOOGLEAI_Studio_API_KEY）
NEXT_PUBLIC_MODE=opensource
